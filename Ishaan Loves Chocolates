int chocolates(int arr[], int n)
{
    // Complete the function
    
    int lo = 0 , hi = n-1;
    
    while(lo < hi){
        
        if(arr[lo] > arr[hi]){
            
            lo++;
        }
        
        else{
            
            hi--;
        }
    }
    
    return arr[lo];
}
